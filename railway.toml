[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd network-ui && npm install && npm run build && cd .."

[deploy]
startCommand = "bash start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
